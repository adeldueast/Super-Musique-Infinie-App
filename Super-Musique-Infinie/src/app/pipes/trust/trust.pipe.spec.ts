/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TrustPipe } from './trust.pipe';

describe('Pipe: Truste', () => {
  it('create an instance', () => {
    let pipe = new TrustPipe();
    expect(pipe).toBeTruthy();
  });
});
